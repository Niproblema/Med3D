Shared session with shared VPT parameters + gc in shared session
MCC import/export geo fix -streamSaver? -limitfloat32 digits
MCS EnvironmentMap when MCC is projected


Maybe:
 -VPT geometry switch causes GL drawArrays error in first draw. Move vptInterface further up in MainPass
 -Move vptInterface into it's own render pass?
 -blend VPT renderers
 -Second subscriber rejoin - duplicate shared cameras on host.

Rethink: 
 -Mip background off maybe broken