/**
 * Manager for shared and user specific VPT volume render settings
 */
M3D.VPTManager = class {
    // ============================ LIFECYCLE ============================ //
/*     constructor(prd, gl) {
        CommonUtils.extend(this);
        this._setupVars();
        this._publicRenderData = prd;
        this._gl = gl;
    } */

}